apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prom-stack.fullname" . }}-test-connection"
  labels:
    {{- include "prom-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prom-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
