apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart-db.fullname" . }}
  labels:
    {{- include "cart-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cart-db.selectorLabels" . | nindent 4 }}
