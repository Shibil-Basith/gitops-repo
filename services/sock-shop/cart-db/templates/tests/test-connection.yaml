apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cart-db.fullname" . }}-test-connection"
  labels:
    {{- include "cart-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cart-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
